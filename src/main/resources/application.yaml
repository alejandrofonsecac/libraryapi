spring:
  datasource:
    url: jdbc:mysql://localhost:3310/libraryapi?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      dll-auto:update
    show-sql: true